'use client';

import { Button } from '@mantine/core';

export function TestComp(props: any) {
    return (
        <Button size="xs" variant="subtle">
            TestComp
        </Button>
    );
}
